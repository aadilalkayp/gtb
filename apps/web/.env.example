# Supabase (browser-safe public keys only).
VITE_SUPABASE_URL="https://<project-ref>.supabase.co"
VITE_SUPABASE_ANON_KEY="<anon-key>"

# Base URL of the Next.js API (the VPS backend).
VITE_API_URL="http://localhost:3001"
